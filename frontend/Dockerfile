FROM alpine:3.20

WORKDIR /workspace
COPY . .

# Replace this file with your chosen frontend production image and startup command.
CMD ["sh", "-c", "echo 'Frontend production Dockerfile placeholder'; sleep infinity"]
