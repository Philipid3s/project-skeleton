FROM alpine:3.20

WORKDIR /workspace
COPY . .

# Replace this file with your chosen backend production image and startup command.
CMD ["sh", "-c", "echo 'Backend production Dockerfile placeholder'; sleep infinity"]
