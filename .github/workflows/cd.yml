name: CD

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deployment Placeholder
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Configure deployment
        run: |
          echo "This repository ships with deployment placeholders."
          echo "Replace this workflow with the target environment's build and deploy steps."
