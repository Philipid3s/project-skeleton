name: CD

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4

      - name: Build & push Docker images
        run: |
          echo "TODO: configure registry push"
          # docker buildx build --push ...

      - name: Deploy
        run: |
          echo "TODO: configure deployment step"
          # e.g. kubectl rollout / SSH / etc.
